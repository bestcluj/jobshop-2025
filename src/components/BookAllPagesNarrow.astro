---
import BookPageNarrow from "./BookPageNarrow.astro";
const { numberOfPages } = Astro.props;
---

{Array.from({length: numberOfPages}).map(
    
    (val, index) => {
        return <BookPageNarrow
            pageNumber={index + 1}
            img=`/meet_the_team/Raul.svg`
        />
    }
)}